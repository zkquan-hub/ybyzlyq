# 那些年，我们的延边一中

## 应用概览

延边第一中学的校友留言墙网站，校友可以留下文字寄语、上传照片，共同回忆在延边一中度过的青春岁月。

## 功能模块

- 留言展示墙：卡片式展示所有校友留言
- 发布留言：支持文字内容 + 图片链接
- 留言持久化：刷新页面后留言不丢失

## 设计规范

### 风格定位

- 青春校园风格，清爽的蓝色系，充满书卷气与回忆感
- 主色调：深邃的校徽蓝色，搭配洁白背景
- 整体感觉：干净、明亮、有青春的朝气

### 色彩系统

- 主色：`hsl(210, 80%, 45%)` 校徽蓝
- 辅助色：`hsl(200, 75%, 55%)` 天蓝色
- 背景色：`hsl(210, 30%, 96%)` 淡蓝灰
- 卡片背景：`hsl(0, 0%, 100%)` 纯白
- 文字主色：`hsl(210, 30%, 20%)` 深蓝灰
- 文字次色：`hsl(210, 15%, 50%)` 浅蓝灰
- 边框/分隔：`hsl(210, 20%, 90%)` 淡蓝边

### 排版层级

- 标题 `text-3xl font-bold`
- 副标题 `text-xl font-semibold`
- 正文 `text-base`
- 辅助文字 `text-sm`
- 小字说明 `text-xs`

### 间距规范

- 页面水平内边距：`px-6`（移动端）/ `px-12`（桌面端）
- 区块间距：`space-y-8`
- 卡片内边距：`p-5`
- 卡片间距：`gap-4`

### 组件规范

- 留言卡片：圆角 `rounded-2xl`，阴影柔和 `shadow-sm`，hover 时轻微上浮
- 按钮：主按钮使用校徽蓝色，圆角 `rounded-full`，有过渡动效
- 输入框：圆角 `rounded-xl`，边框淡蓝边，focus 时主色光晕
- 图片：圆角 `rounded-xl`，object-cover
